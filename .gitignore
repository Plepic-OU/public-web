.idea/.DS_Store
node_modules/

# Environment variables (NEVER commit)
.env
*.env
.claude/.ralph-loop.local.md

# Test artifacts
tests/results/
tests/report/
playwright-report/
test-results/
.lighthouseci/

# TypeScript build artifacts
scripts/dist/

# Analytics credentials (NEVER commit)
analytics/credentials/*.json

# Ephemeral design/iteration artifacts
screenshots/
demo-tenders-screenshot.png
# impeccable/critique screenshot output + live-server runtime state
# (.impeccable/critique/ and live/config.json stay tracked)
.critique-shots/
.impeccable/live/server.json

# Design comps: may hold unpublished photography awaiting consent. Never deploy.
.impeccable/comps/

# Claude Design canvas: the seeded page is a 2 MB build artifact (the bundled
# design skill's editor payload plus the artboards). Rebuild it from the
# tracked sources in design-canvas/ rather than committing it.
design-canvas/plepic-design-system.html
